#!/bin/bash

set -u

regex="$1"

emacsclient --no-wait \
    --eval "(progn (dan/search \"$regex\" \"$PWD\") (select-frame-set-input-focus (selected-frame)) (other-window 1) (delete-other-windows))" > /dev/null
